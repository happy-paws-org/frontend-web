name: Frontend Web CI

on:
  push:
    branches:
      - develop
      - main
  pull_request:

jobs:
  ci:
    uses: happy-paws-org/happy-paws-platform/.github/workflows/frontend-ci.yaml@main
    with:
      service_name: frontend-web